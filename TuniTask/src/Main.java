import entity.Quizs;

public class Main {
    public static void main(String[] args) {
        // Create a quiz to insert
     Quizs quiz = new Quizs(2,"java Quiz", "This is a java quiz");
//        Quizs quiz2 = new Quizs("android Quiz", "This is an android quiz");
//        // Create an instance of ServiceQuizs
//        ServiceQuizs serviceQuizs = new ServiceQuizs();

       // serviceQuizs.insert(quiz);
   // serviceQuizs.insert(quiz2);
      //  quiz.setId_quiz(2);
//        quiz.setId_quiz(1);
//        quiz.setQuiz_title("java");
//        quiz.setQuiz_description("Updated Quiz Description");
//        quiz.setScore(10);
//        System.out.println("Quiz id: " + quiz.getId_quiz());
//        System.out.println("Updated Quiz: " + quiz);
//        serviceQuizs.update(quiz, quiz.getId_quiz());

//        quiz2.setId_quiz(9);
//        serviceQuizs.delete(quiz2);
//        System.out.println(serviceQuizs.readAll());

//        ServiceQuestions serviceQuestions = new ServiceQuestions();
       // serviceQuestions.insert(q1);
//        q1.setId_question(1);
//        serviceQuestions.delete(q1);
//q1.setId_question(10);
//        q1.setQuestion_text("should i learn java ?");
//
//        q1.setQuiz_id(quiz);
//        serviceQuestions.update(q1, q1.getId_question());


        // create a new question to use for testing

      //  Questions question = new Questions(10, "What is Java?", quiz);

       // Reponses response = new Reponses( "A programming language",true, question);

        // test the create method
    //    serviceReponses.insert(response);


//response.setId_reponse(6);
//        response.setReponse_text("An object-oriented programming language");
//        serviceReponses.update(response, response.getId_reponse());
//        System.out.println("Updated Response: " + response);
       // serviceReponses.delete(response);

    }
}
